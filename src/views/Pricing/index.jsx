import styles from "./Pricing.module.scss";

function Pricing() {
  return (
    <>
      <section className={`${styles["section"]} ${styles.pricing}`}></section>
    </>
  );
}

export default Pricing;
